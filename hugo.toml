baseURL = 'https://silent.engineer/'
defaultContentLanguage = 'en'
title = 'rohit gudi · silent.engineer'
ignoreLogs = ['warning-goldmark-raw-html']

# No theme — native layouts under /layouts/
# theme = ""

[params]
  brand = "R.GUDI"
  handle = "silent.engineer"
  description = "10-year platform/infrastructure engineer. Supply chain security, Kubernetes, workload identity, zero-knowledge proofs, automation. Technical notes on infrastructure, software and — sometimes — equity research."
  author = "Rohit Gudi"

  # Socials surfaced in footer + bio block
  github_url   = "https://github.com/caretak3r"
  linkedin_url = "https://www.linkedin.com/in/rohit-gudi-142093359/"
  x_url        = "https://x.com/caretak8r"
  email_url    = "mailto:gudi.k.rohit@gmail.com"

# ---- Top-bar nav (the 'view switcher' — 3 primary sections + brand) ----
[menu]
  [[menu.main]]
    name   = "HOME"
    url    = "/"
    weight = 10
  [[menu.main]]
    name   = "ENGINEERING"
    url    = "/engineering/"
    weight = 20
  [[menu.main]]
    name   = "PROJECTS"
    url    = "/projects/"
    weight = 25
  [[menu.main]]
    name   = "RESEARCH"
    url    = "/research/"
    weight = 30
  [[menu.main]]
    name   = "FINANCIALS"
    url    = "/financials/"
    weight = 35

# ---- Security ----
# Hugo 0.164 flipped the default to deny text/html page content
# (security.allowContent = ['! ^text/html$']). The research reports under
# content/research/*/index.html are authored as raw HTML, so allow it back.
[security]
  allowContent = ['.*']

# ---- Markdown render config ----
[markup]
  [markup.goldmark.renderer]
    unsafe = true                 # allow raw HTML in MD (needed for embedded <p align="center"> images)

  [markup.goldmark.parser.attribute]
    block = true
    title = true

  [markup.tableOfContents]
    startLevel = 2
    endLevel   = 3
    ordered    = false

  # Chroma syntax highlighting via class-based output;
  # the actual colors come from assets/css/chroma-tan.css (generated in Phase 2).
  [markup.highlight]
    codeFences        = true
    guessSyntax       = true
    lineNos           = false
    noClasses         = false
    tabWidth          = 2

# ---- Taxonomies ----
[taxonomies]
  category = 'categories'
  tag      = 'tags'

# ---- Permalinks (preserve existing /posts/SLUG/ URLs) ----
[permalinks]
  posts    = "/posts/:slug/"
  projects = "/projects/:slug/"
  research = "/research/:slug/"

# ---- Build / output ----
[outputs]
  home    = ["HTML", "RSS"]
  section = ["HTML", "RSS"]

# ---- Minify ----
[minify]
  disableHTML = false
  minifyOutput = false
